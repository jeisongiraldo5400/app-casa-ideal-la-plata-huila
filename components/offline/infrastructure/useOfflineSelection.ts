/**
 * Atajo de importación del hook de marcas por entidad; la implementación vive
 * con el servicio de preferencias (`syncPrefsService.ts`).
 */
export {
  useOfflineSelection,
  type OfflineSelection,
  type SyncMode,
  type SyncPrefDomain,
} from './syncPrefsService';
